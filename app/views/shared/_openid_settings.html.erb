	<%- if logged_in? -%>
		<div class="right_side">
			<div class="pad">
                                <h3>OpenID Settings</h3>
                                <ul id="navi">
                                        <% unless checkid_request %>
                                       	  <%= nav 'identity', identity_url(current_account), 'accounts' => ['show'] %>
                                          <%= nav 'profile', edit_account_url, 'accounts' => ['edit', 'update'] %>
                                          <%= nav 'personas', account_personas_url, 'personas' => [] %>
                                          <%= nav 'trusted sites', account_sites_url, 'sites' => [] %>
                                        <% else %>
                                          <%= nav 'current verification request', proceed_url, 'server' => [] %>
                                        <% end %>
                                </ul>
			</div>
		</div>
	<%- end -%>


