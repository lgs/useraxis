<h1>Services List</h1>
<br />
<table>
<% for service in @services %>
  <tr>
    <!-- td><# %= link_to "#{image_tag("/images/sites_icons/#{service.servicename}.png")} #{service.servicename}", edit_service_path(service) -%></td -->
    <!-- td><# %= link_to "#{image_tag("/images/sites_icons/#{service}.png")} #{service}", edit_service_path(service) -%></td -->
    <td><%= link_to image_tag("/images/sites_icons/#{service}.png"), service -%><%= link_to service, edit_service_path(Service.find_by_servicename(service)) -%></td>

  </tr>
<% end %>
</table>

<%= link_to 'New service', new_service_path %>



